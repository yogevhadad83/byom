services:
  - type: web
    name: byom-chat
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: BYOM_API
        value: https://chat-hub-ybyy.onrender.com
