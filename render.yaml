services:
  - type: web
    name: byom-chat
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: BYOM_API
        value: https://byom-api.onrender.com
