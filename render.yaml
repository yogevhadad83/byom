services:
  - type: web
    name: byom-chat
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: SAAS_BASE_URL
        value: https://byom-api.onrender.com
